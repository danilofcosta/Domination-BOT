import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */ allowedDevOrigins: ['phototropic-sierra-untouchably.ngrok-free.dev'],
  reactCompiler: true,
};

export default nextConfig;
